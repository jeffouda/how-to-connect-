This is how to create new repository and connect it with local machine.
Go to github account and create a new repository that is - jeff/the name you like to call it.
Create a folder you will glone it to.
Inside the folder create at least a file.
Git initialize the folder.
git commit -m"give a message"
git remote add (paste the link of the new repository created)
git push -u origin main

